A simple Todo app is developed with bootstrap and materialize in ruby on rails. 
-> to run app just clone or download the repo.
and then 
  
  cd todo_app-by-rails
  bundle install
  rails server (or) rails s
  
